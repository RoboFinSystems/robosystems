"""
LadybugDB Schema Extensions

Domain schemas that extend the base schema with domain-specific node types
and relationships. Product extensions (roboledger, roboinvestor) grow beyond
the graph schema into full applications with OLTP databases, API routes,
data pipelines, and dedicated frontends.
"""
