Description: RoboSystems Dagster Orchestration - ECS Fargate Only

Parameters:
  # Environment
  Environment:
    Type: String
    Default: prod
    AllowedValues:
      - prod
      - staging
    Description: Environment name (prod or staging)
    ConstraintDescription: Must be prod or staging

  # Infrastructure & Networking
  VpcId:
    Type: AWS::EC2::VPC::Id
    Description: VPC ID for ECS tasks
  SubnetIds:
    Type: List<AWS::EC2::Subnet::Id>
    Description: Private subnet IDs for ECS tasks (comma-separated)

  # Container & Application Configuration
  ECRRepositoryUrl:
    Type: String
    Description: Full ECR repository URL (e.g., 123456789.dkr.ecr.us-east-1.amazonaws.com/robosystems)
  ECRImageTag:
    Type: String
    Description: Docker image tag
    Default: latest

  # Fargate Configuration
  # Valid CPU/Memory combinations:
  # 512 CPU: 1024-4096 MB | 1024 CPU: 2048-8192 MB | 2048 CPU: 4096-16384 MB
  DaemonCpu:
    Type: String
    Default: "1024"
    Description: CPU units for Dagster daemon (1024 = 1 vCPU)
    AllowedValues: ["512", "1024", "2048"]
  DaemonMemory:
    Type: String
    Default: "2048"
    Description: Memory in MiB for Dagster daemon
    AllowedValues: ["2048", "4096", "8192"]
  WebserverCpu:
    Type: String
    Default: "512"
    Description: CPU units for Dagster webserver (512 = 0.5 vCPU)
    AllowedValues: ["512", "1024", "2048"]
  WebserverMemory:
    Type: String
    Default: "1024"
    Description: Memory in MiB for Dagster webserver
    AllowedValues: ["1024", "2048", "4096"]
  # Run Job Profiles (Fargate)
  # Four tiers to match workload requirements:
  #   Light   (0.5 vCPU / 2 GB)  - sensors, simple jobs, most runs
  #   Standard (2 vCPU / 8 GB)   - normal ETL, moderate processing
  #   Compute  (4 vCPU / 16 GB)  - daily SEC processing, staging
  #   Heavy   (16 vCPU / 64 GB)  - SEC backfills, batch_limit=8000+
  #
  # Valid Fargate CPU/Memory combinations:
  #   512:  1024-4096 MB   | 1024:  2048-8192 MB   | 2048:  4096-16384 MB
  #   4096: 8192-30720 MB  | 8192: 16384-61440 MB  | 16384: 32768-122880 MB

  # Light profile (default) - most Dagster jobs
  LightRunJobCpu:
    Type: String
    Default: "512"
    Description: CPU units for light run jobs (512 = 0.5 vCPU)
    AllowedValues: ["256", "512", "1024"]
  LightRunJobMemory:
    Type: String
    Default: "2048"
    Description: Memory in MiB for light run jobs
    AllowedValues: ["1024", "2048", "4096"]

  # Standard profile - moderate workloads
  StandardRunJobCpu:
    Type: String
    Default: "2048"
    Description: CPU units for standard run jobs (2048 = 2 vCPU)
    AllowedValues: ["1024", "2048", "4096"]
  StandardRunJobMemory:
    Type: String
    Default: "8192"
    Description: Memory in MiB for standard run jobs
    AllowedValues: ["4096", "8192", "16384"]

  # Compute profile - daily SEC processing, staging
  ComputeRunJobCpu:
    Type: String
    Default: "4096"
    Description: CPU units for compute run jobs (4096 = 4 vCPU)
    AllowedValues: ["2048", "4096", "8192"]
  ComputeRunJobMemory:
    Type: String
    Default: "16384"
    Description: Memory in MiB for compute run jobs
    AllowedValues: ["8192", "16384", "30720"]

  # Heavy profile - SEC backfills, large batches
  HeavyRunJobCpu:
    Type: String
    Default: "16384"
    Description: CPU units for heavy run jobs (16384 = 16 vCPU)
    AllowedValues: ["8192", "16384"]
  HeavyRunJobMemory:
    Type: String
    Default: "65536"
    Description: Memory in MiB for heavy run jobs (64 GB)
    AllowedValues: ["32768", "65536", "122880"]

  MaxConcurrentRuns:
    Type: Number
    Default: 20
    MinValue: 1
    MaxValue: 200
    Description: Maximum concurrent Dagster runs (QueuedRunCoordinator)

  # Capacity Provider Configuration (Spot vs On-Demand)
  # Daemon: Orchestration process - interruption causes brief delay in job scheduling
  DaemonFargateWeight:
    Type: Number
    Default: 20
    MinValue: 0
    MaxValue: 100
    Description: Weight for FARGATE (On-Demand) capacity provider for daemon
  DaemonFargateSpotWeight:
    Type: Number
    Default: 80
    MinValue: 0
    MaxValue: 100
    Description: Weight for FARGATE_SPOT capacity provider for daemon
  # Webserver: UI and bastion tunnel access - configurable Spot ratio
  WebserverFargateWeight:
    Type: Number
    Default: 20
    MinValue: 0
    MaxValue: 100
    Description: Weight for FARGATE (On-Demand) capacity provider for webserver
  WebserverFargateSpotWeight:
    Type: Number
    Default: 80
    MinValue: 0
    MaxValue: 100
    Description: Weight for FARGATE_SPOT capacity provider for webserver

  # Base (minimum guaranteed On-Demand tasks) - set to 1+ for guaranteed availability
  DaemonFargateBase:
    Type: Number
    Default: 0
    MinValue: 0
    MaxValue: 5
    Description: Minimum daemon tasks guaranteed on FARGATE (On-Demand)
  WebserverFargateBase:
    Type: Number
    Default: 0
    MinValue: 0
    MaxValue: 5
    Description: Minimum webserver tasks guaranteed on FARGATE (On-Demand)

  # Cache Configuration
  ValkeyUrl:
    Type: String
    Description: Valkey ElastiCache endpoint URL
    AllowedPattern: ^rediss?://.*:[0-9]+$
  ValkeyClientSecurityGroupId:
    Type: String
    Description: Security group ID for applications connecting to Valkey
    AllowedPattern: ^sg-[0-9a-f]+$

  # Database Configuration (uses existing RDS)
  DatabaseSecurityGroupId:
    Type: String
    Description: Security group ID for PostgreSQL access
    AllowedPattern: ^sg-[0-9a-f]+$

  # Bastion Access (for port forwarding)
  BastionSecurityGroupId:
    Type: String
    Description: Security group ID for bastion host access
    AllowedPattern: ^(sg-[0-9a-f]+)?$
    Default: ""

  # API Access (for job submission via GraphQL)
  ApiSecurityGroupId:
    Type: String
    Description: Security group ID for API service to submit Dagster jobs
    AllowedPattern: ^(sg-[0-9a-f]+)?$
    Default: ""

  # Monitoring
  LogRetentionDays:
    Type: Number
    Default: 14
    MinValue: 1
    MaxValue: 365
    Description: CloudWatch log retention in days
  ContainerInsightsEnabled:
    Type: String
    Default: disabled
    AllowedValues: ["enabled", "disabled"]
    Description: Enable/disable ECS Container Insights

  # Tagging Configuration
  ServiceTag:
    Type: String
    Default: RoboSystems
    Description: Service tag for resource identification and billing
  ComponentTag:
    Type: String
    Default: Dagster
    Description: Component tag for resource identification and billing

  # S3 Bucket Configuration (from S3 stack outputs)
  SharedRawBucketArn:
    Type: String
    Description: ARN of shared raw data bucket (from S3 stack output)
  SharedProcessedBucketArn:
    Type: String
    Description: ARN of shared processed data bucket (from S3 stack output)
  DeploymentBucketArn:
    Type: String
    Description: ARN of deployment bucket (from S3 stack output)
  UserDataBucketArn:
    Type: String
    Description: ARN of user data bucket (from S3 stack output)
  PublicDataBucketArn:
    Type: String
    Description: ARN of public data bucket (from S3 stack output)
  LogsBucketArn:
    Type: String
    Description: ARN of logs bucket for Dagster compute logs (from S3 stack output)
  PublicDataCDNURL:
    Type: String
    Description: CloudFront CDN URL for public data (from S3 stack output)

Conditions:
  HasBastionAccess: !Not [!Equals [!Ref BastionSecurityGroupId, ""]]
  HasApiAccess: !Not [!Equals [!Ref ApiSecurityGroupId, ""]]

Resources:
  # ========================================
  # SERVICE DISCOVERY (Internal DNS)
  # ========================================
  DagsterNamespace:
    Type: AWS::ServiceDiscovery::PrivateDnsNamespace
    Properties:
      Name: !Sub dagster.${Environment}.robosystems.local
      Vpc: !Ref VpcId
      Description: !Sub Private DNS namespace for Dagster services (${Environment})
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Service
          Value: !Ref ServiceTag
        - Key: Component
          Value: !Ref ComponentTag

  WebserverServiceDiscovery:
    Type: AWS::ServiceDiscovery::Service
    Properties:
      Name: webserver
      NamespaceId: !Ref DagsterNamespace
      DnsConfig:
        DnsRecords:
          - Type: A
            TTL: 10
        RoutingPolicy: MULTIVALUE
      HealthCheckCustomConfig:
        FailureThreshold: 1
      Description: Dagster webserver service discovery
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Service
          Value: !Ref ServiceTag
        - Key: Component
          Value: !Ref ComponentTag
        - Key: CreatedBy
          Value: CloudFormation

  # ========================================
  # ECS CLUSTER (Fargate Only)
  # ========================================
  DagsterCluster:
    Type: AWS::ECS::Cluster
    DeletionPolicy: Delete
    UpdateReplacePolicy: Retain
    Properties:
      ClusterName: !Sub robosystems-dagster-${Environment}-cluster
      ClusterSettings:
        - Name: containerInsights
          Value: !Ref ContainerInsightsEnabled
      CapacityProviders:
        - FARGATE
        - FARGATE_SPOT
      DefaultCapacityProviderStrategy:
        - CapacityProvider: FARGATE_SPOT
          Weight: 80
        - CapacityProvider: FARGATE
          Weight: 20
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Service
          Value: !Ref ServiceTag
        - Key: Component
          Value: !Ref ComponentTag
        - Key: CreatedBy
          Value: CloudFormation

  # ========================================
  # SECURITY GROUPS
  # ========================================
  DagsterSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Security group for Dagster ECS tasks
      VpcId: !Ref VpcId
      SecurityGroupEgress:
        - IpProtocol: -1
          CidrIp: 0.0.0.0/0
      Tags:
        - Key: Name
          Value: !Sub sg-robosystems-dagster-${Environment}
        - Key: Environment
          Value: !Ref Environment
        - Key: Service
          Value: !Ref ServiceTag
        - Key: Component
          Value: !Ref ComponentTag

  # Allow internal communication between Dagster components
  DagsterInternalIngress:
    Type: AWS::EC2::SecurityGroupIngress
    Properties:
      GroupId: !Ref DagsterSecurityGroup
      IpProtocol: -1
      SourceSecurityGroupId: !Ref DagsterSecurityGroup
      Description: Allow internal Dagster component communication

  # Allow bastion to access webserver (for port forwarding)
  BastionToDagsterIngress:
    Type: AWS::EC2::SecurityGroupIngress
    Condition: HasBastionAccess
    Properties:
      GroupId: !Ref DagsterSecurityGroup
      IpProtocol: tcp
      FromPort: 3000
      ToPort: 3000
      SourceSecurityGroupId: !Ref BastionSecurityGroupId
      Description: Allow bastion host to access Dagster webserver

  # Allow API to submit jobs to Dagster webserver (GraphQL)
  ApiToDagsterIngress:
    Type: AWS::EC2::SecurityGroupIngress
    Condition: HasApiAccess
    Properties:
      GroupId: !Ref DagsterSecurityGroup
      IpProtocol: tcp
      FromPort: 3000
      ToPort: 3000
      SourceSecurityGroupId: !Ref ApiSecurityGroupId
      Description: Allow API service to submit Dagster jobs via GraphQL

  # ========================================
  # LOG GROUPS
  # ========================================
  DagsterLogGroup:
    Type: AWS::Logs::LogGroup
    DeletionPolicy: Delete
    Properties:
      LogGroupName: !Sub /robosystems/${Environment}/dagster
      RetentionInDays: !Ref LogRetentionDays
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Service
          Value: !Ref ServiceTag
        - Key: Component
          Value: !Ref ComponentTag

  # ========================================
  # IAM ROLES
  # ========================================
  DagsterTaskRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub robosystems-dagster-task-role-${Environment}
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: ecs-tasks.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: DagsterTaskPolicy
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              # Secrets Manager - GetSecretValue with resource restrictions
              - Effect: Allow
                Action:
                  - secretsmanager:GetSecretValue
                  - secretsmanager:DescribeSecret
                Resource:
                  - !Sub "arn:${AWS::Partition}:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:robosystems/${Environment}-*"
                  - !Sub "arn:${AWS::Partition}:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:robosystems/${Environment}/*"
              # Secrets Manager - ListSecrets for ECS run launcher tag-based discovery
              # Note: ListSecrets doesn't support resource-level permissions
              - Effect: Allow
                Action:
                  - secretsmanager:ListSecrets
                Resource: "*"
              # KMS for secrets
              - Effect: Allow
                Action:
                  - kms:Decrypt
                  - kms:DescribeKey
                Resource:
                  - !Sub "arn:${AWS::Partition}:kms:${AWS::Region}:${AWS::AccountId}:alias/aws/secretsmanager"
                Condition:
                  StringEquals:
                    "kms:ViaService":
                      - !Sub "secretsmanager.${AWS::Region}.amazonaws.com"
              # S3 for shared data pipelines and deployments
              - Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:PutObject
                  - s3:DeleteObject
                  - s3:ListBucket
                  - s3:GetBucketLocation
                Resource:
                  - !Ref SharedRawBucketArn
                  - !Sub "${SharedRawBucketArn}/*"
                  - !Ref SharedProcessedBucketArn
                  - !Sub "${SharedProcessedBucketArn}/*"
                  - !Ref DeploymentBucketArn
                  - !Sub "${DeploymentBucketArn}/*"
                  - !Ref UserDataBucketArn
                  - !Sub "${UserDataBucketArn}/*"
                  - !Ref PublicDataBucketArn
                  - !Sub "${PublicDataBucketArn}/*"
                  - !Ref LogsBucketArn
                  - !Sub "${LogsBucketArn}/*"
              # DynamoDB for graph registries (needed for shared master discovery)
              - Effect: Allow
                Action:
                  - dynamodb:Scan
                  - dynamodb:Query
                  - dynamodb:GetItem
                  - dynamodb:PutItem
                  - dynamodb:UpdateItem
                  - dynamodb:DeleteItem
                  - dynamodb:BatchGetItem
                  - dynamodb:BatchWriteItem
                  - dynamodb:DescribeTable
                Resource:
                  - !Sub "arn:${AWS::Partition}:dynamodb:${AWS::Region}:${AWS::AccountId}:table/robosystems-graph-${Environment}-*"
                  - !Sub "arn:${AWS::Partition}:dynamodb:${AWS::Region}:${AWS::AccountId}:table/robosystems-graph-${Environment}-*/index/*"
              # EC2 for snapshot management and ECS run launcher
              - Effect: Allow
                Action:
                  - ec2:CreateSnapshot
                  - ec2:DescribeSnapshots
                  - ec2:CreateTags
                  - ec2:DescribeVolumes
                  - ec2:DescribeInstances
                  - ec2:DescribeNetworkInterfaces
                Resource: "*"
              # EC2 Launch Template management (for replica updates)
              - Effect: Allow
                Action:
                  - ec2:CreateLaunchTemplateVersion
                  - ec2:ModifyLaunchTemplate
                  - ec2:DescribeLaunchTemplates
                  - ec2:DescribeLaunchTemplateVersions
                Resource:
                  - !Sub "arn:${AWS::Partition}:ec2:${AWS::Region}:${AWS::AccountId}:launch-template/*"
              # Auto Scaling for replica refresh
              - Effect: Allow
                Action:
                  - autoscaling:DescribeAutoScalingGroups
                  - autoscaling:UpdateAutoScalingGroup
                  - autoscaling:StartInstanceRefresh
                  - autoscaling:DescribeInstanceRefreshes
                Resource: "*"
              # ECS for run launcher
              - Effect: Allow
                Action:
                  - ecs:RunTask
                  - ecs:DescribeTasks
                  - ecs:StopTask
                  - ecs:ListTasks
                  - ecs:DescribeTaskDefinition
                  - ecs:RegisterTaskDefinition
                  - ecs:DeregisterTaskDefinition
                  - ecs:DescribeServices
                  - ecs:TagResource
                Resource: "*"
              # IAM pass role for task execution
              - Effect: Allow
                Action:
                  - iam:PassRole
                Resource:
                  - !GetAtt DagsterTaskExecutionRole.Arn
                  - !Sub "arn:${AWS::Partition}:iam::${AWS::AccountId}:role/robosystems-dagster-task-role-${Environment}"
                Condition:
                  StringEquals:
                    "iam:PassedToService": "ecs-tasks.amazonaws.com"
              # CloudWatch Logs
              - Effect: Allow
                Action:
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                  - logs:DescribeLogStreams
                Resource:
                  - !Sub "arn:${AWS::Partition}:logs:${AWS::Region}:${AWS::AccountId}:log-group:/robosystems/${Environment}/dagster:*"
              # CloudWatch Metrics for instance monitoring and auto-scaling
              - Effect: Allow
                Action:
                  - cloudwatch:PutMetricData
                Resource: "*"
                Condition:
                  StringEquals:
                    "cloudwatch:namespace": !Sub "RoboSystems/Graph/${Environment}"
              # SES for transactional emails (verification, password reset, welcome)
              - Effect: Allow
                Action:
                  - ses:SendEmail
                Resource:
                  - !Sub "arn:${AWS::Partition}:ses:${AWS::Region}:${AWS::AccountId}:identity/robosystems.ai"
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Service
          Value: !Ref ServiceTag
        - Key: Component
          Value: !Ref ComponentTag

  DagsterTaskExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub robosystems-dagster-execution-role-${Environment}
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: ecs-tasks.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - !Sub arn:${AWS::Partition}:iam::aws:policy/service-role/AmazonECSTaskExecutionRolePolicy
      Policies:
        - PolicyName: ExecutionRoleSecrets
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - secretsmanager:GetSecretValue
                  - secretsmanager:DescribeSecret
                Resource:
                  - !Sub "arn:${AWS::Partition}:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:robosystems/${Environment}-*"
                  - !Sub "arn:${AWS::Partition}:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:robosystems/${Environment}/*"
              - Effect: Allow
                Action:
                  - kms:Decrypt
                  - kms:DescribeKey
                Resource:
                  - !Sub "arn:${AWS::Partition}:kms:${AWS::Region}:${AWS::AccountId}:alias/aws/secretsmanager"
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Service
          Value: !Ref ServiceTag
        - Key: Component
          Value: !Ref ComponentTag
        - Key: CreatedBy
          Value: CloudFormation

  # ========================================
  # TASK DEFINITIONS
  # ========================================
  DaemonTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub robosystems-dagster-daemon-${Environment}
      Cpu: !Ref DaemonCpu
      Memory: !Ref DaemonMemory
      NetworkMode: awsvpc
      RequiresCompatibilities:
        - FARGATE
      RuntimePlatform:
        CpuArchitecture: ARM64
        OperatingSystemFamily: LINUX
      ExecutionRoleArn: !GetAtt DagsterTaskExecutionRole.Arn
      TaskRoleArn: !GetAtt DagsterTaskRole.Arn
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Service
          Value: !Ref ServiceTag
        - Key: Component
          Value: !Ref ComponentTag
        - Key: CreatedBy
          Value: CloudFormation
      ContainerDefinitions:
        - Name: dagster-daemon
          Image: !Sub ${ECRRepositoryUrl}:${ECRImageTag}
          Essential: true
          Command: ["/app/bin/entrypoint.sh"]
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref DagsterLogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: daemon
          Environment:
            - Name: ENVIRONMENT
              Value: !Ref Environment
            - Name: DOCKER_PROFILE
              Value: dagster-daemon
            - Name: DAGSTER_HOME
              Value: /app/dagster_home
            - Name: DAGSTER_POSTGRES_HOST
              Value: !Sub "{{resolve:secretsmanager:robosystems/${Environment}/postgres:SecretString:host}}"
            - Name: DAGSTER_POSTGRES_PORT
              Value: "5432"
            - Name: DAGSTER_POSTGRES_USER
              Value: !Sub "{{resolve:secretsmanager:robosystems/${Environment}/postgres:SecretString:username}}"
            - Name: DAGSTER_POSTGRES_PASSWORD
              Value: !Sub "{{resolve:secretsmanager:robosystems/${Environment}/postgres:SecretString:password}}"
            - Name: DAGSTER_POSTGRES_DB
              Value: dagster
            - Name: VALKEY_URL
              Value: !Ref ValkeyUrl
            - Name: AWS_REGION
              Value: !Ref AWS::Region
            # Run database migrations on daemon startup (singleton pattern)
            - Name: RUN_MIGRATIONS
              Value: "true"
            # EcsRunLauncher configuration - Fargate only
            # Default task definition is Light profile (most jobs)
            - Name: DAGSTER_ECS_RUN_TASK_DEFINITION
              Value: !GetAtt LightRunJobTaskDefinition.TaskDefinitionArn
            - Name: DAGSTER_ECS_CLUSTER
              Value: !Ref DagsterCluster
            - Name: DAGSTER_ECS_LAUNCH_TYPE
              Value: FARGATE
            # Run job profile task definitions (for ecs/task_definition tag selection)
            - Name: DAGSTER_ECS_TASK_DEFINITION_LIGHT
              Value: !GetAtt LightRunJobTaskDefinition.TaskDefinitionArn
            - Name: DAGSTER_ECS_TASK_DEFINITION_STANDARD
              Value: !GetAtt StandardRunJobTaskDefinition.TaskDefinitionArn
            - Name: DAGSTER_ECS_TASK_DEFINITION_COMPUTE
              Value: !GetAtt ComputeRunJobTaskDefinition.TaskDefinitionArn
            - Name: DAGSTER_ECS_TASK_DEFINITION_HEAVY
              Value: !GetAtt HeavyRunJobTaskDefinition.TaskDefinitionArn
            # S3 Bucket Configuration (derived from ARN parameters)
            - Name: SHARED_RAW_BUCKET
              Value: !Select [5, !Split [":", !Ref SharedRawBucketArn]]
            - Name: SHARED_PROCESSED_BUCKET
              Value: !Select [5, !Split [":", !Ref SharedProcessedBucketArn]]
            - Name: USER_DATA_BUCKET
              Value: !Select [5, !Split [":", !Ref UserDataBucketArn]]
            - Name: PUBLIC_DATA_BUCKET
              Value: !Select [5, !Split [":", !Ref PublicDataBucketArn]]
            - Name: DEPLOYMENT_BUCKET
              Value: !Select [5, !Split [":", !Ref DeploymentBucketArn]]
            - Name: LOGS_BUCKET
              Value: !Select [5, !Split [":", !Ref LogsBucketArn]]
            - Name: DAGSTER_COMPUTE_LOGS_BUCKET
              Value: !Select [5, !Split [":", !Ref LogsBucketArn]]
            - Name: PUBLIC_DATA_CDN_URL
              Value: !Ref PublicDataCDNURL
            # Run coordinator configuration
            - Name: DAGSTER_MAX_CONCURRENT_RUNS
              Value: !Ref MaxConcurrentRuns

  WebserverTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub robosystems-dagster-webserver-${Environment}
      Cpu: !Ref WebserverCpu
      Memory: !Ref WebserverMemory
      NetworkMode: awsvpc
      RequiresCompatibilities:
        - FARGATE
      RuntimePlatform:
        CpuArchitecture: ARM64
        OperatingSystemFamily: LINUX
      ExecutionRoleArn: !GetAtt DagsterTaskExecutionRole.Arn
      TaskRoleArn: !GetAtt DagsterTaskRole.Arn
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Service
          Value: !Ref ServiceTag
        - Key: Component
          Value: !Ref ComponentTag
        - Key: CreatedBy
          Value: CloudFormation
      ContainerDefinitions:
        - Name: dagster-webserver
          Image: !Sub ${ECRRepositoryUrl}:${ECRImageTag}
          Essential: true
          Command: ["/app/bin/entrypoint.sh"]
          PortMappings:
            - ContainerPort: 3000
              Protocol: tcp
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref DagsterLogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: webserver
          Environment:
            - Name: ENVIRONMENT
              Value: !Ref Environment
            - Name: DOCKER_PROFILE
              Value: dagster
            - Name: DAGSTER_HOME
              Value: /app/dagster_home
            - Name: DAGSTER_PORT
              Value: "3000"
            - Name: DAGSTER_POSTGRES_HOST
              Value: !Sub "{{resolve:secretsmanager:robosystems/${Environment}/postgres:SecretString:host}}"
            - Name: DAGSTER_POSTGRES_PORT
              Value: "5432"
            - Name: DAGSTER_POSTGRES_USER
              Value: !Sub "{{resolve:secretsmanager:robosystems/${Environment}/postgres:SecretString:username}}"
            - Name: DAGSTER_POSTGRES_PASSWORD
              Value: !Sub "{{resolve:secretsmanager:robosystems/${Environment}/postgres:SecretString:password}}"
            - Name: DAGSTER_POSTGRES_DB
              Value: dagster
            - Name: VALKEY_URL
              Value: !Ref ValkeyUrl
            - Name: AWS_REGION
              Value: !Ref AWS::Region
            # EcsRunLauncher configuration - needed for webserver to understand run launcher
            - Name: DAGSTER_ECS_RUN_TASK_DEFINITION
              Value: !GetAtt LightRunJobTaskDefinition.TaskDefinitionArn
            - Name: DAGSTER_ECS_CLUSTER
              Value: !Ref DagsterCluster
            - Name: DAGSTER_ECS_LAUNCH_TYPE
              Value: FARGATE
            # Run job profile task definitions (for ecs/task_definition tag selection)
            - Name: DAGSTER_ECS_TASK_DEFINITION_LIGHT
              Value: !GetAtt LightRunJobTaskDefinition.TaskDefinitionArn
            - Name: DAGSTER_ECS_TASK_DEFINITION_STANDARD
              Value: !GetAtt StandardRunJobTaskDefinition.TaskDefinitionArn
            - Name: DAGSTER_ECS_TASK_DEFINITION_COMPUTE
              Value: !GetAtt ComputeRunJobTaskDefinition.TaskDefinitionArn
            - Name: DAGSTER_ECS_TASK_DEFINITION_HEAVY
              Value: !GetAtt HeavyRunJobTaskDefinition.TaskDefinitionArn
            # S3 Bucket Configuration (derived from ARN parameters)
            - Name: SHARED_RAW_BUCKET
              Value: !Select [5, !Split [":", !Ref SharedRawBucketArn]]
            - Name: SHARED_PROCESSED_BUCKET
              Value: !Select [5, !Split [":", !Ref SharedProcessedBucketArn]]
            - Name: USER_DATA_BUCKET
              Value: !Select [5, !Split [":", !Ref UserDataBucketArn]]
            - Name: PUBLIC_DATA_BUCKET
              Value: !Select [5, !Split [":", !Ref PublicDataBucketArn]]
            - Name: DEPLOYMENT_BUCKET
              Value: !Select [5, !Split [":", !Ref DeploymentBucketArn]]
            - Name: LOGS_BUCKET
              Value: !Select [5, !Split [":", !Ref LogsBucketArn]]
            - Name: DAGSTER_COMPUTE_LOGS_BUCKET
              Value: !Select [5, !Split [":", !Ref LogsBucketArn]]
            - Name: PUBLIC_DATA_CDN_URL
              Value: !Ref PublicDataCDNURL
            # Run coordinator configuration (shared config with daemon)
            - Name: DAGSTER_MAX_CONCURRENT_RUNS
              Value: !Ref MaxConcurrentRuns
          HealthCheck:
            Command:
              [
                "CMD-SHELL",
                "curl -f http://localhost:3000/server_info || exit 1",
              ]
            Interval: 30
            Timeout: 10
            Retries: 3
            StartPeriod: 60

  # ========================================
  # RUN JOB TASK DEFINITIONS (4 profiles)
  # ========================================
  # Jobs select profile via ecs/task_definition tag. Default: Light
  # Light: sensors, simple jobs | Standard: moderate ETL | Compute: SEC daily | Heavy: SEC backfill

  # Light profile - Default for most jobs (0.5 vCPU, 2 GB, 21 GB storage)
  LightRunJobTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub robosystems-dagster-run-light-${Environment}
      Cpu: !Ref LightRunJobCpu
      Memory: !Ref LightRunJobMemory
      NetworkMode: awsvpc
      RequiresCompatibilities:
        - FARGATE
      RuntimePlatform:
        CpuArchitecture: ARM64
        OperatingSystemFamily: LINUX
      ExecutionRoleArn: !GetAtt DagsterTaskExecutionRole.Arn
      TaskRoleArn: !GetAtt DagsterTaskRole.Arn
      # Default 21GB ephemeral storage (sufficient for light jobs)
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Service
          Value: !Ref ServiceTag
        - Key: Component
          Value: !Ref ComponentTag
        - Key: Profile
          Value: light
        - Key: CreatedBy
          Value: CloudFormation
      ContainerDefinitions:
        - Name: dagster-run
          Image: !Sub ${ECRRepositoryUrl}:${ECRImageTag}
          Essential: true
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref DagsterLogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: run-light
          Environment:
            - Name: ENVIRONMENT
              Value: !Ref Environment
            - Name: DAGSTER_HOME
              Value: /app/dagster_home
            - Name: DAGSTER_POSTGRES_HOST
              Value: !Sub "{{resolve:secretsmanager:robosystems/${Environment}/postgres:SecretString:host}}"
            - Name: DAGSTER_POSTGRES_PORT
              Value: "5432"
            - Name: DAGSTER_POSTGRES_USER
              Value: !Sub "{{resolve:secretsmanager:robosystems/${Environment}/postgres:SecretString:username}}"
            - Name: DAGSTER_POSTGRES_PASSWORD
              Value: !Sub "{{resolve:secretsmanager:robosystems/${Environment}/postgres:SecretString:password}}"
            - Name: DAGSTER_POSTGRES_DB
              Value: dagster
            - Name: VALKEY_URL
              Value: !Ref ValkeyUrl
            - Name: AWS_REGION
              Value: !Ref AWS::Region
            - Name: SHARED_RAW_BUCKET
              Value: !Select [5, !Split [":", !Ref SharedRawBucketArn]]
            - Name: SHARED_PROCESSED_BUCKET
              Value: !Select [5, !Split [":", !Ref SharedProcessedBucketArn]]
            - Name: USER_DATA_BUCKET
              Value: !Select [5, !Split [":", !Ref UserDataBucketArn]]
            - Name: PUBLIC_DATA_BUCKET
              Value: !Select [5, !Split [":", !Ref PublicDataBucketArn]]
            - Name: DEPLOYMENT_BUCKET
              Value: !Select [5, !Split [":", !Ref DeploymentBucketArn]]
            - Name: LOGS_BUCKET
              Value: !Select [5, !Split [":", !Ref LogsBucketArn]]
            - Name: DAGSTER_COMPUTE_LOGS_BUCKET
              Value: !Select [5, !Split [":", !Ref LogsBucketArn]]
            - Name: PUBLIC_DATA_CDN_URL
              Value: !Ref PublicDataCDNURL
            - Name: DAGSTER_MAX_CONCURRENT_RUNS
              Value: !Ref MaxConcurrentRuns

  # Standard profile - Moderate workloads (2 vCPU, 8 GB, 50 GB storage)
  StandardRunJobTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub robosystems-dagster-run-standard-${Environment}
      Cpu: !Ref StandardRunJobCpu
      Memory: !Ref StandardRunJobMemory
      NetworkMode: awsvpc
      RequiresCompatibilities:
        - FARGATE
      RuntimePlatform:
        CpuArchitecture: ARM64
        OperatingSystemFamily: LINUX
      ExecutionRoleArn: !GetAtt DagsterTaskExecutionRole.Arn
      TaskRoleArn: !GetAtt DagsterTaskRole.Arn
      EphemeralStorage:
        SizeInGiB: 50
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Service
          Value: !Ref ServiceTag
        - Key: Component
          Value: !Ref ComponentTag
        - Key: Profile
          Value: standard
        - Key: CreatedBy
          Value: CloudFormation
      ContainerDefinitions:
        - Name: dagster-run
          Image: !Sub ${ECRRepositoryUrl}:${ECRImageTag}
          Essential: true
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref DagsterLogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: run-standard
          Environment:
            - Name: ENVIRONMENT
              Value: !Ref Environment
            - Name: DAGSTER_HOME
              Value: /app/dagster_home
            - Name: DAGSTER_POSTGRES_HOST
              Value: !Sub "{{resolve:secretsmanager:robosystems/${Environment}/postgres:SecretString:host}}"
            - Name: DAGSTER_POSTGRES_PORT
              Value: "5432"
            - Name: DAGSTER_POSTGRES_USER
              Value: !Sub "{{resolve:secretsmanager:robosystems/${Environment}/postgres:SecretString:username}}"
            - Name: DAGSTER_POSTGRES_PASSWORD
              Value: !Sub "{{resolve:secretsmanager:robosystems/${Environment}/postgres:SecretString:password}}"
            - Name: DAGSTER_POSTGRES_DB
              Value: dagster
            - Name: VALKEY_URL
              Value: !Ref ValkeyUrl
            - Name: AWS_REGION
              Value: !Ref AWS::Region
            - Name: SHARED_RAW_BUCKET
              Value: !Select [5, !Split [":", !Ref SharedRawBucketArn]]
            - Name: SHARED_PROCESSED_BUCKET
              Value: !Select [5, !Split [":", !Ref SharedProcessedBucketArn]]
            - Name: USER_DATA_BUCKET
              Value: !Select [5, !Split [":", !Ref UserDataBucketArn]]
            - Name: PUBLIC_DATA_BUCKET
              Value: !Select [5, !Split [":", !Ref PublicDataBucketArn]]
            - Name: DEPLOYMENT_BUCKET
              Value: !Select [5, !Split [":", !Ref DeploymentBucketArn]]
            - Name: LOGS_BUCKET
              Value: !Select [5, !Split [":", !Ref LogsBucketArn]]
            - Name: DAGSTER_COMPUTE_LOGS_BUCKET
              Value: !Select [5, !Split [":", !Ref LogsBucketArn]]
            - Name: PUBLIC_DATA_CDN_URL
              Value: !Ref PublicDataCDNURL
            - Name: DAGSTER_MAX_CONCURRENT_RUNS
              Value: !Ref MaxConcurrentRuns

  # Compute profile - Daily SEC processing, staging (4 vCPU, 16 GB, 100 GB storage)
  ComputeRunJobTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub robosystems-dagster-run-compute-${Environment}
      Cpu: !Ref ComputeRunJobCpu
      Memory: !Ref ComputeRunJobMemory
      NetworkMode: awsvpc
      RequiresCompatibilities:
        - FARGATE
      RuntimePlatform:
        CpuArchitecture: ARM64
        OperatingSystemFamily: LINUX
      ExecutionRoleArn: !GetAtt DagsterTaskExecutionRole.Arn
      TaskRoleArn: !GetAtt DagsterTaskRole.Arn
      EphemeralStorage:
        SizeInGiB: 100
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Service
          Value: !Ref ServiceTag
        - Key: Component
          Value: !Ref ComponentTag
        - Key: Profile
          Value: compute
        - Key: CreatedBy
          Value: CloudFormation
      ContainerDefinitions:
        - Name: dagster-run
          Image: !Sub ${ECRRepositoryUrl}:${ECRImageTag}
          Essential: true
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref DagsterLogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: run-compute
          Environment:
            - Name: ENVIRONMENT
              Value: !Ref Environment
            - Name: DAGSTER_HOME
              Value: /app/dagster_home
            - Name: DAGSTER_POSTGRES_HOST
              Value: !Sub "{{resolve:secretsmanager:robosystems/${Environment}/postgres:SecretString:host}}"
            - Name: DAGSTER_POSTGRES_PORT
              Value: "5432"
            - Name: DAGSTER_POSTGRES_USER
              Value: !Sub "{{resolve:secretsmanager:robosystems/${Environment}/postgres:SecretString:username}}"
            - Name: DAGSTER_POSTGRES_PASSWORD
              Value: !Sub "{{resolve:secretsmanager:robosystems/${Environment}/postgres:SecretString:password}}"
            - Name: DAGSTER_POSTGRES_DB
              Value: dagster
            - Name: VALKEY_URL
              Value: !Ref ValkeyUrl
            - Name: AWS_REGION
              Value: !Ref AWS::Region
            - Name: SHARED_RAW_BUCKET
              Value: !Select [5, !Split [":", !Ref SharedRawBucketArn]]
            - Name: SHARED_PROCESSED_BUCKET
              Value: !Select [5, !Split [":", !Ref SharedProcessedBucketArn]]
            - Name: USER_DATA_BUCKET
              Value: !Select [5, !Split [":", !Ref UserDataBucketArn]]
            - Name: PUBLIC_DATA_BUCKET
              Value: !Select [5, !Split [":", !Ref PublicDataBucketArn]]
            - Name: DEPLOYMENT_BUCKET
              Value: !Select [5, !Split [":", !Ref DeploymentBucketArn]]
            - Name: LOGS_BUCKET
              Value: !Select [5, !Split [":", !Ref LogsBucketArn]]
            - Name: DAGSTER_COMPUTE_LOGS_BUCKET
              Value: !Select [5, !Split [":", !Ref LogsBucketArn]]
            - Name: PUBLIC_DATA_CDN_URL
              Value: !Ref PublicDataCDNURL
            - Name: DAGSTER_MAX_CONCURRENT_RUNS
              Value: !Ref MaxConcurrentRuns

  # Heavy profile - SEC backfills, large batches (16 vCPU, 64 GB, 200 GB storage)
  HeavyRunJobTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub robosystems-dagster-run-heavy-${Environment}
      Cpu: !Ref HeavyRunJobCpu
      Memory: !Ref HeavyRunJobMemory
      NetworkMode: awsvpc
      RequiresCompatibilities:
        - FARGATE
      RuntimePlatform:
        CpuArchitecture: ARM64
        OperatingSystemFamily: LINUX
      ExecutionRoleArn: !GetAtt DagsterTaskExecutionRole.Arn
      TaskRoleArn: !GetAtt DagsterTaskRole.Arn
      EphemeralStorage:
        SizeInGiB: 200
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Service
          Value: !Ref ServiceTag
        - Key: Component
          Value: !Ref ComponentTag
        - Key: Profile
          Value: heavy
        - Key: CreatedBy
          Value: CloudFormation
      ContainerDefinitions:
        - Name: dagster-run
          Image: !Sub ${ECRRepositoryUrl}:${ECRImageTag}
          Essential: true
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref DagsterLogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: run-heavy
          Environment:
            - Name: ENVIRONMENT
              Value: !Ref Environment
            - Name: DAGSTER_HOME
              Value: /app/dagster_home
            - Name: DAGSTER_POSTGRES_HOST
              Value: !Sub "{{resolve:secretsmanager:robosystems/${Environment}/postgres:SecretString:host}}"
            - Name: DAGSTER_POSTGRES_PORT
              Value: "5432"
            - Name: DAGSTER_POSTGRES_USER
              Value: !Sub "{{resolve:secretsmanager:robosystems/${Environment}/postgres:SecretString:username}}"
            - Name: DAGSTER_POSTGRES_PASSWORD
              Value: !Sub "{{resolve:secretsmanager:robosystems/${Environment}/postgres:SecretString:password}}"
            - Name: DAGSTER_POSTGRES_DB
              Value: dagster
            - Name: VALKEY_URL
              Value: !Ref ValkeyUrl
            - Name: AWS_REGION
              Value: !Ref AWS::Region
            - Name: SHARED_RAW_BUCKET
              Value: !Select [5, !Split [":", !Ref SharedRawBucketArn]]
            - Name: SHARED_PROCESSED_BUCKET
              Value: !Select [5, !Split [":", !Ref SharedProcessedBucketArn]]
            - Name: USER_DATA_BUCKET
              Value: !Select [5, !Split [":", !Ref UserDataBucketArn]]
            - Name: PUBLIC_DATA_BUCKET
              Value: !Select [5, !Split [":", !Ref PublicDataBucketArn]]
            - Name: DEPLOYMENT_BUCKET
              Value: !Select [5, !Split [":", !Ref DeploymentBucketArn]]
            - Name: LOGS_BUCKET
              Value: !Select [5, !Split [":", !Ref LogsBucketArn]]
            - Name: DAGSTER_COMPUTE_LOGS_BUCKET
              Value: !Select [5, !Split [":", !Ref LogsBucketArn]]
            - Name: PUBLIC_DATA_CDN_URL
              Value: !Ref PublicDataCDNURL
            - Name: DAGSTER_MAX_CONCURRENT_RUNS
              Value: !Ref MaxConcurrentRuns

  # ========================================
  # ECS SERVICES
  # ========================================
  DaemonService:
    Type: AWS::ECS::Service
    Properties:
      ServiceName: !Sub robosystems-dagster-daemon-${Environment}
      Cluster: !Ref DagsterCluster
      DesiredCount: 1
      CapacityProviderStrategy:
        - CapacityProvider: FARGATE_SPOT
          Weight: !Ref DaemonFargateSpotWeight
        - CapacityProvider: FARGATE
          Weight: !Ref DaemonFargateWeight
          Base: !Ref DaemonFargateBase
      NetworkConfiguration:
        AwsvpcConfiguration:
          AssignPublicIp: DISABLED
          Subnets: !Ref SubnetIds
          SecurityGroups:
            - !Ref DagsterSecurityGroup
            - !Ref ValkeyClientSecurityGroupId
            - !Ref DatabaseSecurityGroupId
      TaskDefinition: !Ref DaemonTaskDefinition
      EnableExecuteCommand: true
      PropagateTags: SERVICE
      DeploymentConfiguration:
        MaximumPercent: 100
        MinimumHealthyPercent: 0
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Service
          Value: !Ref ServiceTag
        - Key: Component
          Value: !Ref ComponentTag
        - Key: CreatedBy
          Value: CloudFormation

  WebserverService:
    Type: AWS::ECS::Service
    Properties:
      ServiceName: !Sub robosystems-dagster-webserver-${Environment}
      Cluster: !Ref DagsterCluster
      DesiredCount: 1 # Always 1 - no ALB for load distribution
      CapacityProviderStrategy:
        - CapacityProvider: FARGATE_SPOT
          Weight: !Ref WebserverFargateSpotWeight
        - CapacityProvider: FARGATE
          Weight: !Ref WebserverFargateWeight
          Base: !Ref WebserverFargateBase
      NetworkConfiguration:
        AwsvpcConfiguration:
          AssignPublicIp: DISABLED
          Subnets: !Ref SubnetIds
          SecurityGroups:
            - !Ref DagsterSecurityGroup
            - !Ref ValkeyClientSecurityGroupId
            - !Ref DatabaseSecurityGroupId
      TaskDefinition: !Ref WebserverTaskDefinition
      # Service Discovery registration
      ServiceRegistries:
        - RegistryArn: !GetAtt WebserverServiceDiscovery.Arn
      EnableExecuteCommand: true
      PropagateTags: SERVICE
      DeploymentConfiguration:
        DeploymentCircuitBreaker:
          Enable: true
          Rollback: true
        MaximumPercent: 200
        MinimumHealthyPercent: 100
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Service
          Value: !Ref ServiceTag
        - Key: Component
          Value: !Ref ComponentTag
        - Key: CreatedBy
          Value: CloudFormation

Outputs:
  ClusterName:
    Description: Name of the ECS Cluster
    Value: !Ref DagsterCluster
    Export:
      Name: !Sub ${AWS::StackName}-ClusterName

  ClusterArn:
    Description: ARN of the ECS Cluster
    Value: !GetAtt DagsterCluster.Arn
    Export:
      Name: !Sub ${AWS::StackName}-ClusterArn

  # Internal access endpoint (use with bastion port forwarding)
  DagsterInternalEndpoint:
    Description: Internal DNS endpoint for Dagster webserver (use with bastion tunnel)
    Value: !Sub webserver.dagster.${Environment}.robosystems.local
    Export:
      Name: !Sub ${AWS::StackName}-InternalEndpoint

  DagsterSecurityGroupId:
    Description: Security group ID for Dagster services
    Value: !Ref DagsterSecurityGroup
    Export:
      Name: !Sub ${AWS::StackName}-SecurityGroupId

  # Run Job Profile Task Definitions
  LightRunJobTaskDefinitionArn:
    Description: ARN of the light run job task definition (default - 0.5 vCPU, 2 GB)
    Value: !GetAtt LightRunJobTaskDefinition.TaskDefinitionArn
    Export:
      Name: !Sub ${AWS::StackName}-LightRunJobTaskDefinition

  StandardRunJobTaskDefinitionArn:
    Description: ARN of the standard run job task definition (2 vCPU, 8 GB)
    Value: !GetAtt StandardRunJobTaskDefinition.TaskDefinitionArn
    Export:
      Name: !Sub ${AWS::StackName}-StandardRunJobTaskDefinition

  ComputeRunJobTaskDefinitionArn:
    Description: ARN of the compute run job task definition (4 vCPU, 16 GB)
    Value: !GetAtt ComputeRunJobTaskDefinition.TaskDefinitionArn
    Export:
      Name: !Sub ${AWS::StackName}-ComputeRunJobTaskDefinition

  HeavyRunJobTaskDefinitionArn:
    Description: ARN of the heavy run job task definition (16 vCPU, 64 GB)
    Value: !GetAtt HeavyRunJobTaskDefinition.TaskDefinitionArn
    Export:
      Name: !Sub ${AWS::StackName}-HeavyRunJobTaskDefinition

  DaemonServiceArn:
    Description: ARN of the Dagster daemon ECS service
    Value: !Ref DaemonService
    Export:
      Name: !Sub ${AWS::StackName}-DaemonServiceArn

  WebserverServiceArn:
    Description: ARN of the Dagster webserver ECS service
    Value: !Ref WebserverService
    Export:
      Name: !Sub ${AWS::StackName}-WebserverServiceArn

  DagsterUrl:
    Description: Internal URL for Dagster webserver
    Value: !Sub http://webserver.dagster.${Environment}.robosystems.local:3000
    Export:
      Name: !Sub ${AWS::StackName}-DagsterUrl

  TunnelCommand:
    Description: Command to access Dagster UI via bastion tunnel
    Value: !Sub "./bin/tools/tunnels.sh ${Environment} dagster"
